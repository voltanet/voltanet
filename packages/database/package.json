{
  "name": "@repo/database",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "format": "biome format --write",
    "lint": "biome lint --write",
    "check": "tsgo --noEmit"
  },
  "devDependencies": {
    "@repo/shared": "workspace:*",
    "@repo/validation": "workspace:*"
  }
}
