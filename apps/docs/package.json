{
  "name": "@repo/docs",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "format": "biome format --write",
    "lint": "biome check --write",
    "check": "tsgo --noEmit"
  },
  "devDependencies": {
    "@repo/shared": "workspace:*"
  }
}
